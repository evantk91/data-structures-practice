function towerOfHanoi(num) {
   //you are given the number of disks as an input
   //write a recursive function that prints the "move" steps in order to move
   //the number of disks from source to destination using the help of the auxillary rod 
   
   let moves = [];

   towerOfHanoiSoln(num,)


}

function towerOfHanoiSoln(num, source, auxilliary, destination) {
   
}

module.exports = {
   towerOfHanoi,    
}